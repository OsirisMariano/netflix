name: netflix-clone

services:
  api:
    build:
      context: ./server
    environment:
      PORT: 4000
    expose:
      - "4000"
    restart: unless-stopped

  web:
    build:
      context: .
    ports:
      - "8080:80"
    depends_on:
      - api
    restart: unless-stopped
